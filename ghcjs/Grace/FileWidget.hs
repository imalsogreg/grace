-- | 

module Grace.FileWidget where

import qualified Data.Text as Text

-- toImageValue :: JSVal -> IO Text.Text
-- toImageValue fileInput = do
--   -- new fileSyncReader
--   -- load
--   -- on loadFinished, read
--   -- on readFinished, return (toBase64 $ the read bytes)
--   undefined
